<%= turbo_stream.replace "idea", partial: "ideas/idea", locals: { idea: @idea, text: JsonService.new(input: {message: @idea.description}).generate} %>
<%= turbo_stream.replace "spinner", SpinnerComponent.new(id: "spinner", class: "hidden") %>